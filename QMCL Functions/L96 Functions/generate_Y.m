% function will accept inputs
        % {omega_i} - the set of training data (cardinality n). Represented by a matrix
            %Each omega_i is an element
            %of the state space (in the L63 case, this will be R^3). It has
            %the property that for the deterministic dynamics operator Xi^(delta*t),that 
            % w_i = Xi^(n * delta* t)(w_0)